export type Feedback = {
  name: string;
  email: string;
  feedback: string;
};

export type FeedbackData = {
  id: number;
  name: string;
  email: string;
  feedback: string;
  createdAt: string;
  UpdatedAt: string;
};
